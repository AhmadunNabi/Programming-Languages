let fruits = ['banana', 'apple', 'orange', 'mango']
// fruits = fruits.sort(); // Alphabetic order
fruits = fruits.sort().reverse(); // Reverse alphabetic order

for (fruit of fruits) {
    console.log(fruit);
}